package algorithms;

public interface Algorithm {
    //fill the makeMove function in each algorithm
    //the function returns a string as the direction of movement (up, down, left or right)
    public String makeMove(String[][] grid);
}
